---
const WEB3FORMS_KEY = import.meta.env.PUBLIC_WEB3FORMS_KEY;
---

<section class="relative overflow-hidden">
  <!-- Background -->
  <div class="pointer-events-none absolute inset-0">
    <div
      class="absolute -top-40 left-1/2 h-[520px] w-[520px] -translate-x-1/2 rounded-full blur-3xl opacity-40 bg-gradient-to-tr from-slate-200 via-white to-slate-200 dark:from-slate-800 dark:via-slate-900 dark:to-slate-800"
    >
    </div>
    <div
      class="absolute -bottom-48 left-1/2 h-[640px] w-[640px] -translate-x-1/2 rounded-full blur-3xl opacity-30 bg-gradient-to-tr from-slate-200 via-white to-slate-200 dark:from-slate-800 dark:via-slate-900 dark:to-slate-800"
    >
    </div>
    <div class="absolute inset-0 opacity-[0.06] dark:opacity-[0.12] grid-overlay"></div>
  </div>

  <div class="mx-auto max-w-6xl px-6 pt-14 pb-10 sm:pt-20">
    <!-- Header -->
    <header class="flex items-center justify-between">
      <div class="flex items-center gap-3">
        <div class="logo-badge" aria-hidden="true">
          <span class="logo-mark">&lt;\&gt;</span>
        </div>
        <div class="leading-tight">
          <div class="text-base font-semibold tracking-tight text-slate-900 dark:text-white">logic.exe</div>
          <div class="text-xs text-slate-600 dark:text-slate-400">Landing Pages • UI/UX • Conversion</div>
        </div>
      </div>

      <a
        href="#offer"
        class="rounded-full border border-slate-200 bg-white/60 px-4 py-2 text-sm font-medium text-slate-900 backdrop-blur hover:bg-white dark:border-slate-800 dark:bg-slate-900/60 dark:text-white dark:hover:bg-slate-900"
      >
        اطلب صفحة الآن
      </a>
    </header>

    <!-- Hero -->
    <div class="mt-14 grid gap-10 lg:grid-cols-2 lg:items-center">
      <div>
        <p
          class="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white/60 px-3 py-1 text-xs font-medium text-slate-700 backdrop-blur dark:border-slate-800 dark:bg-slate-900/60 dark:text-slate-200"
        >
          <span class="dot"></span>
          جودة تصميم عالية + أداء + تجربة مستخدم
        </p>

        <h1 class="mt-5 text-4xl font-semibold tracking-tight text-slate-900 dark:text-white sm:text-5xl">
          logic.exe
          <span class="block text-slate-600 dark:text-slate-300">صفحات هبوط احترافية تُحوّل الزوّار إلى عملاء</span>
        </h1>

        <p class="mt-5 max-w-xl text-base leading-relaxed text-slate-600 dark:text-slate-300">
          نحن في <span class="font-semibold text-slate-900 dark:text-white">logic.exe</span> نركز على بناء صفحات هبوط مصممة
          بعناية: رسالة واضحة، هيكل قوي، وواجهة جميلة وسريعة — لأن الانطباع الأول لازم يكون مثالي، والنتيجة لازم تكون ملموسة.
        </p>

        <div class="mt-7 flex flex-wrap items-center gap-3">
          <a
            href="#packages"
            class="rounded-xl bg-slate-900 px-5 py-3 text-sm font-semibold text-white shadow-sm hover:opacity-95 dark:bg-white dark:text-slate-900"
          >
            شاهد العروض
          </a>

          <a
            href="#offer"
            class="rounded-xl border border-slate-200 bg-white/60 px-5 py-3 text-sm font-semibold text-slate-900 backdrop-blur hover:bg-white dark:border-slate-800 dark:bg-slate-900/60 dark:text-white dark:hover:bg-slate-900"
          >
            قدّم عرضك السعري
          </a>

          <div class="text-xs text-slate-500 dark:text-slate-400">بدون أسعار ثابتة — أنت تحدد السعر الذي يناسبك.</div>
        </div>

        <dl class="mt-9 grid grid-cols-3 gap-4 max-w-xl">
          <div
            class="rounded-2xl border border-slate-200 bg-white/60 p-4 backdrop-blur dark:border-slate-800 dark:bg-slate-900/60"
          >
            <dt class="text-xs text-slate-600 dark:text-slate-400">Design</dt>
            <dd class="mt-1 text-sm font-semibold text-slate-900 dark:text-white">Premium UI</dd>
          </div>
          <div
            class="rounded-2xl border border-slate-200 bg-white/60 p-4 backdrop-blur dark:border-slate-800 dark:bg-slate-900/60"
          >
            <dt class="text-xs text-slate-600 dark:text-slate-400">Performance</dt>
            <dd class="mt-1 text-sm font-semibold text-slate-900 dark:text-white">Fast & SEO</dd>
          </div>
          <div
            class="rounded-2xl border border-slate-200 bg-white/60 p-4 backdrop-blur dark:border-slate-800 dark:bg-slate-900/60"
          >
            <dt class="text-xs text-slate-600 dark:text-slate-400">Conversion</dt>
            <dd class="mt-1 text-sm font-semibold text-slate-900 dark:text-white">CTA-first</dd>
          </div>
        </dl>
      </div>

      <!-- Visual -->
      <div class="relative">
        <div
          class="rounded-3xl border border-slate-200 bg-white/60 p-8 shadow-sm backdrop-blur dark:border-slate-800 dark:bg-slate-900/60"
        >
          <div class="flex items-center justify-between">
            <div class="text-sm font-semibold text-slate-900 dark:text-white">logic.exe — Preview</div>
            <div class="flex gap-2">
              <span class="h-2.5 w-2.5 rounded-full bg-slate-300 dark:bg-slate-700"></span>
              <span class="h-2.5 w-2.5 rounded-full bg-slate-300 dark:bg-slate-700"></span>
              <span class="h-2.5 w-2.5 rounded-full bg-slate-300 dark:bg-slate-700"></span>
            </div>
          </div>

          <div class="mt-8 grid gap-4">
            <div class="h-12 rounded-2xl bg-slate-900/90 dark:bg-white/90"></div>
            <div class="grid grid-cols-3 gap-4">
              <div class="h-24 rounded-2xl bg-slate-200 dark:bg-slate-800"></div>
              <div class="h-24 rounded-2xl bg-slate-200 dark:bg-slate-800"></div>
              <div class="h-24 rounded-2xl bg-slate-200 dark:bg-slate-800"></div>
            </div>
            <div class="h-12 rounded-2xl bg-slate-200 dark:bg-slate-800"></div>
          </div>

          <p class="mt-6 text-sm text-slate-600 dark:text-slate-300">
            واجهة بسيطة — ولكن مدروسة: هرمية معلومات واضحة، تباين ممتاز، وCTA قوي.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<section id="packages" class="mx-auto max-w-6xl px-6 py-14">
  <div class="flex items-end justify-between gap-6">
    <div>
      <h2 class="text-2xl font-semibold tracking-tight text-slate-900 dark:text-white">عروض صفحات الهبوط</h2>
      <p class="mt-2 max-w-2xl text-sm leading-relaxed text-slate-600 dark:text-slate-300">
        ثلاث باقات واضحة مثل أسلوب Fiverr — لكن مبنية على جودة فعلية: تصميم، تجربة مستخدم، ونظام محتوى منظم.
        <span class="font-semibold text-slate-900 dark:text-white">لا نحدد سعر.</span> أنت تقدم العرض الذي يناسبك.
      </p>
    </div>
    <a
      href="#offer"
      class="hidden sm:inline-flex rounded-xl border border-slate-200 bg-white/60 px-4 py-2 text-sm font-semibold text-slate-900 backdrop-blur hover:bg-white dark:border-slate-800 dark:bg-slate-900/60 dark:text-white dark:hover:bg-slate-900"
    >
      قدّم عرضك
    </a>
  </div>

  <div class="mt-8 grid gap-6 lg:grid-cols-3">
    <article
      class="group rounded-3xl border border-slate-200 bg-white/60 p-7 backdrop-blur transition hover:-translate-y-0.5 hover:bg-white dark:border-slate-800 dark:bg-slate-900/60 dark:hover:bg-slate-900"
    >
      <div class="flex items-center justify-between">
        <h3 class="text-lg font-semibold text-slate-900 dark:text-white">Starter Landing</h3>
        <span
          class="rounded-full border border-slate-200 bg-white px-3 py-1 text-xs font-medium text-slate-700 dark:border-slate-800 dark:bg-slate-950 dark:text-slate-200"
        >
          1 صفحة
        </span>
      </div>
      <p class="mt-3 text-sm text-slate-600 dark:text-slate-300">
        صفحة هبوط نظيفة وسريعة: قسم Hero قوي، أقسام فوائد، شهادات/ضمان، CTA واضح، ونموذج تواصل.
      </p>
      <ul class="mt-5 space-y-2 text-sm text-slate-700 dark:text-slate-200">
        <li class="flex gap-2"><span class="check"></span> بنية محتوى واضحة</li>
        <li class="flex gap-2"><span class="check"></span> Responsive بالكامل</li>
        <li class="flex gap-2"><span class="check"></span> SEO أساسي + Meta</li>
      </ul>
      <a
        href="#offer"
        class="mt-7 inline-flex w-full items-center justify-center rounded-xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white hover:opacity-95 dark:bg-white dark:text-slate-900"
      >
        قدّم عرضك لهذه الباقة
      </a>
    </article>

    <article
      class="group rounded-3xl border border-slate-200 bg-white/60 p-7 backdrop-blur transition hover:-translate-y-0.5 hover:bg-white dark:border-slate-800 dark:bg-slate-900/60 dark:hover:bg-slate-900"
    >
      <div class="flex items-center justify-between">
        <h3 class="text-lg font-semibold text-slate-900 dark:text-white">Pro Conversion</h3>
        <span
          class="rounded-full border border-slate-200 bg-white px-3 py-1 text-xs font-medium text-slate-700 dark:border-slate-800 dark:bg-slate-950 dark:text-slate-200"
        >
          1–2 صفحات
        </span>
      </div>
      <p class="mt-3 text-sm text-slate-600 dark:text-slate-300">
        تصميم يركز على التحويل: أقسام عرض قيمة قوية، مقارنة، FAQ، CTA متعدد، وتجربة قراءة مدروسة.
      </p>
      <ul class="mt-5 space-y-2 text-sm text-slate-700 dark:text-slate-200">
        <li class="flex gap-2"><span class="check"></span> Copy عالي الجودة</li>
        <li class="flex gap-2"><span class="check"></span> أقسام تحويل كاملة</li>
        <li class="flex gap-2"><span class="check"></span> تحسين سرعة + صور</li>
      </ul>
      <a
        href="#offer"
        class="mt-7 inline-flex w-full items-center justify-center rounded-xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white hover:opacity-95 dark:bg-white dark:text-slate-900"
      >
        قدّم عرضك لهذه الباقة
      </a>
    </article>

    <article
      class="group rounded-3xl border border-slate-200 bg-white/60 p-7 backdrop-blur transition hover:-translate-y-0.5 hover:bg-white dark:border-slate-800 dark:bg-slate-900/60 dark:hover:bg-slate-900"
    >
      <div class="flex items-center justify-between">
        <h3 class="text-lg font-semibold text-slate-900 dark:text-white">Elite Brand</h3>
        <span
          class="rounded-full border border-slate-200 bg-white px-3 py-1 text-xs font-medium text-slate-700 dark:border-slate-800 dark:bg-slate-950 dark:text-slate-200"
        >
          تجربة كاملة
        </span>
      </div>
      <p class="mt-3 text-sm text-slate-600 dark:text-slate-300">
        هوية متكاملة داخل الصفحة: حركة احترافية، أقسام عميقة، micro-interactions، وتقديم يعكس علامة قوية.
      </p>
      <ul class="mt-5 space-y-2 text-sm text-slate-700 dark:text-slate-200">
        <li class="flex gap-2"><span class="check"></span> Motion/UI متقدم</li>
        <li class="flex gap-2"><span class="check"></span> Sections مخصصة</li>
        <li class="flex gap-2"><span class="check"></span> تجربة “Premium”</li>
      </ul>
      <a
        href="#offer"
        class="mt-7 inline-flex w-full items-center justify-center rounded-xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white hover:opacity-95 dark:bg-white dark:text-slate-900"
      >
        قدّم عرضك لهذه الباقة
      </a>
    </article>
  </div>
</section>

<section id="offer" class="mx-auto max-w-6xl px-6 pb-16">
  <div
    class="rounded-3xl border border-slate-200 bg-white/60 p-8 backdrop-blur dark:border-slate-800 dark:bg-slate-900/60"
  >
    <div class="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between">
      <div>
        <h2 class="text-2xl font-semibold tracking-tight text-slate-900 dark:text-white">قدّم عرضك السعري الآن</h2>
        <p class="mt-2 text-sm text-slate-600 dark:text-slate-300">
          اكتب تفاصيل مشروعك والسعر الذي يناسبك — وسنرد عليك بوضوح وخطوات تنفيذ.
        </p>
      </div>
      <div class="text-xs text-slate-500 dark:text-slate-400">النموذج يعمل عبر Web3Forms (بدون Backend).</div>
    </div>

    <form class="mt-7 grid gap-4 sm:grid-cols-2" method="POST" action="https://api.web3forms.com/submit">
      <input type="hidden" name="access_key" value={WEB3FORMS_KEY} />
      <input type="hidden" name="subject" value="logic.exe — New Landing Offer" />
      <input type="hidden" name="from_name" value="logic.exe Landing" />

      <label class="grid gap-2">
        <span class="text-sm font-medium text-slate-900 dark:text-white">الاسم</span>
        <input
          name="name"
          required
          class="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 placeholder-slate-400 outline-none ring-0 focus:border-slate-400 dark:border-slate-800 dark:bg-slate-950 dark:text-white dark:placeholder-slate-600"
          placeholder="اسمك الكامل"
        />
      </label>

      <label class="grid gap-2">
        <span class="text-sm font-medium text-slate-900 dark:text-white">البريد الإلكتروني</span>
        <input
          name="email"
          type="email"
          required
          class="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 placeholder-slate-400 outline-none ring-0 focus:border-slate-400 dark:border-slate-800 dark:bg-slate-950 dark:text-white dark:placeholder-slate-600"
          placeholder="name@domain.com"
        />
      </label>

      <label class="grid gap-2 sm:col-span-2">
        <span class="text-sm font-medium text-slate-900 dark:text-white">عرض السعر</span>
        <input
          name="budget"
          required
          class="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 placeholder-slate-400 outline-none ring-0 focus:border-slate-400 dark:border-slate-800 dark:bg-slate-950 dark:text-white dark:placeholder-slate-600"
          placeholder="مثال: 150$ أو 300$ أو حسب نطاق العمل"
        />
      </label>

      <label class="grid gap-2 sm:col-span-2">
        <span class="text-sm font-medium text-slate-900 dark:text-white">تفاصيل المشروع</span>
        <textarea
          name="message"
          required
          rows="5"
          class="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 placeholder-slate-400 outline-none ring-0 focus:border-slate-400 dark:border-slate-800 dark:bg-slate-950 dark:text-white dark:placeholder-slate-600"
          placeholder="ما نوع الصفحة؟ جمهورك؟ ما الذي تريد التركيز عليه؟ روابط أمثلة إن وجدت…"></textarea>
      </label>

      <div class="sm:col-span-2 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <button
          type="submit"
          class="inline-flex items-center justify-center rounded-xl bg-slate-900 px-6 py-3 text-sm font-semibold text-white hover:opacity-95 dark:bg-white dark:text-slate-900"
        >
          إرسال العرض
        </button>

        <p class="text-xs text-slate-500 dark:text-slate-400">بإرسال النموذج أنت توافق على تواصلنا معك عبر البريد.</p>
      </div>
    </form>
  </div>
</section>

<style>
  /* ---------- Base grid overlay ---------- */
  .grid-overlay {
    background-image:
      linear-gradient(to right, rgba(15, 23, 42, 0.25) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(15, 23, 42, 0.25) 1px, transparent 1px);
    background-size: 44px 44px;
    mask-image: radial-gradient(ellipse at center, black 45%, transparent 72%);
  }

  /* ---------- Accent dot ---------- */
  .dot {
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: currentColor;
    box-shadow: 0 0 0 6px rgba(15, 23, 42, 0.08);
  }

  /* ---------- Premium Logo Badge ---------- */
  .logo-badge {
    width: 44px;
    height: 44px;
    border-radius: 14px;
    display: grid;
    place-items: center;
    border: 1px solid rgba(148, 163, 184, 0.35);
    background: rgba(255, 255, 255, 0.65);
    backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;

    /* micro polish */
    transform: translateZ(0);
  }
  :global(.dark) .logo-badge {
    border-color: rgba(51, 65, 85, 0.9);
    background: rgba(2, 6, 23, 0.55);
  }

  /* rotating conic highlight */
  .logo-badge::before {
    content: '';
    position: absolute;
    inset: -60%;
    background: conic-gradient(from 180deg, rgba(15, 23, 42, 0), rgba(15, 23, 42, 0.22), rgba(15, 23, 42, 0));
    animation: spin 2.4s linear infinite;
    will-change: transform;
  }
  :global(.dark) .logo-badge::before {
    background: conic-gradient(from 180deg, rgba(255, 255, 255, 0), rgba(255, 255, 255, 0.18), rgba(255, 255, 255, 0));
  }

  /* inner glass */
  .logo-badge::after {
    content: '';
    position: absolute;
    inset: 1px;
    border-radius: 13px;
    background: inherit;
    backdrop-filter: blur(10px);
  }

  /* mark animation */
  .logo-mark {
    position: relative;
    z-index: 1;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace;
    font-weight: 800;
    letter-spacing: -0.06em;
    font-size: 14px;
    color: rgba(15, 23, 42, 0.92);

    animation: float 2.8s ease-in-out infinite;
    will-change: transform;
    transform: translateZ(0);
  }
  :global(.dark) .logo-mark {
    color: rgba(255, 255, 255, 0.92);
  }

  /* Subtle glow pulse */
  .logo-badge {
    box-shadow:
      0 1px 0 rgba(15, 23, 42, 0.06),
      0 18px 50px rgba(15, 23, 42, 0.08);
    animation: glow 3.2s ease-in-out infinite;
    will-change: box-shadow;
  }
  :global(.dark) .logo-badge {
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.04),
      0 18px 55px rgba(0, 0, 0, 0.35);
  }

  /* ---------- Cards polish ---------- */
  #packages article {
    transform: translateZ(0);
    transition:
      transform 220ms ease,
      box-shadow 220ms ease,
      border-color 220ms ease,
      background-color 220ms ease;
    box-shadow: 0 10px 30px rgba(15, 23, 42, 0.06);
  }
  #packages article:hover {
    transform: translateY(-3px);
    box-shadow: 0 18px 55px rgba(15, 23, 42, 0.12);
    border-color: rgba(148, 163, 184, 0.6);
  }
  :global(.dark) #packages article {
    box-shadow: 0 18px 55px rgba(0, 0, 0, 0.35);
  }
  :global(.dark) #packages article:hover {
    box-shadow: 0 22px 65px rgba(0, 0, 0, 0.45);
    border-color: rgba(51, 65, 85, 0.9);
  }

  /* ---------- Buttons polish (no extra JS) ---------- */
  a,
  button {
    outline: none;
  }
  a:focus-visible,
  button:focus-visible {
    box-shadow: 0 0 0 4px rgba(148, 163, 184, 0.35);
  }
  :global(.dark) a:focus-visible,
  :global(.dark) button:focus-visible {
    box-shadow: 0 0 0 4px rgba(148, 163, 184, 0.18);
  }

  /* press effect */
  a:active,
  button:active {
    transform: translateY(1px);
  }

  /* ---------- Check icon ---------- */
  .check {
    width: 18px;
    height: 18px;
    border-radius: 6px;
    border: 1px solid rgba(148, 163, 184, 0.45);
    display: inline-block;
    position: relative;
    flex: 0 0 auto;
    margin-top: 2px;
  }
  .check::after {
    content: '';
    position: absolute;
    left: 5px;
    top: 3px;
    width: 6px;
    height: 10px;
    border-right: 2px solid rgba(15, 23, 42, 0.8);
    border-bottom: 2px solid rgba(15, 23, 42, 0.8);
    transform: rotate(40deg);
  }
  :global(.dark) .check::after {
    border-right-color: rgba(255, 255, 255, 0.85);
    border-bottom-color: rgba(255, 255, 255, 0.85);
  }

  /* ---------- Motion accessibility ---------- */
  @media (prefers-reduced-motion: reduce) {
    .logo-badge,
    .logo-badge::before,
    .logo-mark {
      animation: none !important;
    }
    #packages article {
      transition: none !important;
    }
  }

  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }
  @keyframes float {
    0%,
    100% {
      transform: translateY(0) rotate(-1deg);
    }
    50% {
      transform: translateY(-3px) rotate(1deg);
    }
  }
  @keyframes glow {
    0%,
    100% {
      box-shadow:
        0 1px 0 rgba(15, 23, 42, 0.06),
        0 18px 50px rgba(15, 23, 42, 0.08);
    }
    50% {
      box-shadow:
        0 1px 0 rgba(15, 23, 42, 0.06),
        0 22px 65px rgba(15, 23, 42, 0.12);
    }
  }
</style>
